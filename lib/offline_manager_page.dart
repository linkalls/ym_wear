import 'package:flutter/material.dart';

class OfflineManagerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('オフライン管理機能（未実装）')),
    );
  }
}
